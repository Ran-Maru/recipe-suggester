// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/javascript-node
{
  "name": "recipe-suggester開発環境(Node.js + Playwright)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  // Playwright公式イメージが用意したユーザーを使用する。
  "remoteUser": "pwuser",
  "postCreateCommand": "sudo chown -R pwuser:pwuser node_modules && npm install",
  // パフォーマンス高速化のためnode_modulesをDocker Volumeに格納する。
  "mounts": [
    "source=${localWorkspaceFolderBasename}-node-modules,target=${containerWorkspaceFolder}/node_modules,type=volume"
  ],

  // Features to add to the dev container. More info: https://containers.dev/features.
  "features": {
    // 最新版のGitを使用するため。/ バージョン固定したくなったら設定を記載予定
    "ghcr.io/devcontainers/features/git": {
      "ppa": "true"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  // Vite, Playwright reporterのデフォルトポート開放
  "forwardPorts": [5173, 9323],
  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      // エディタの見た目以外はここで拡張機能を導入する。
      "extensions": [
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "ms-playwright.playwright",
        "github.vscode-github-actions",
        "streetsidesoftware.code-spell-checker",
        "ms-azuretools.vscode-docker",
        "docker.docker",
        // 以下ドキュメント作成補助用
        "bierner.markdown-preview-github-styles"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "prettier.requireConfig": true
      }
    }
  }
  // Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.e
  // "remoteUser": "root"
}
